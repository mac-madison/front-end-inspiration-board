const CreateBoard = () => {

    // should sumbit the user input fields
    // Board Title and Owners name
    //Button that hides or expands CreateBoard Form

    //WHEN BOARDS EMPTY  SUBMIT BUTTON IS DISABLED
}

export default CreateBoard;
